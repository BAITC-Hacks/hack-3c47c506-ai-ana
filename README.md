# AI-ANA — подбор подрядчиков для событий

Сервис для выбора до трёх event-подрядчиков в Казахстане с понятными объяснениями. Реализован интерфейс и выполнена проверка этапов 1–7: интерфейс, загрузка каталога, подбор, объяснения с доказательствами и API. Сервер читает 66 исходных профилей из CSV, поддерживает JSONL и сохраняет происхождение записей. Подбор работает через Python, CLI и POST /api/recommendations. Форма сайта выполняет реальный запрос к серверу, карточки и основания поступают из API. NVIDIA API не подключён.

## Локальный запуск

Проверенная среда: Node.js 20.19.2, npm 9.2.0 и Python 3.13.5. Сначала запустите сервер на порту 8000 по инструкции «Сервер и каталог» ниже. Затем в другом терминале запустите интерфейс. Зависимости закреплены в `frontend/package-lock.json`.

```bash
cd frontend
npm ci
npm run dev
```

Откройте http://localhost:5173. Если порт занят, Vite сообщит другой адрес в терминале.

```bash
npm run build
npm run preview
```

Предпросмотр сборки: http://localhost:4173; сервер также должен работать. Для остановки сервера нажмите Ctrl+C.

Vite в dev и preview проксирует `/api` на `http://127.0.0.1:8000`. Другой адрес сервера задаётся при запуске: `API_PROXY_TARGET=http://127.0.0.1:8001 npm run dev`. Адрес остаётся в конфигурации Vite; браузер отправляет запросы на тот же origin. При публикации потребуется настроить такой же reverse proxy; публичное развёртывание пока не выполнялось.

## Проверка интерфейса

```bash
cd frontend
npx playwright install chromium
npm run test:e2e
```

На Linux для Chromium могут понадобиться системные библиотеки: `npx playwright install-deps chromium` (установка системных пакетов требует соответствующих прав). Перед запуском установите зависимости сервера (инструкция ниже). Тесты сами запускают отдельные FastAPI и Vite на портах 8002 и 5174, затем останавливают их. Проверяются реальные карточки, смена даты, валидация, ошибки, отмена запоздалых запросов, оформление и четыре ширины; снимки обычного прогона записываются в `docs/screenshots/current-*.png`. Тесты запускаем после завершения этапа, без промежуточных прогонов.

## Что посмотреть

- Форму с обязательными параметрами и необязательными языком и длительностью.
- До трёх реальных карточек с раскрываемыми основаниями и пометками происхождения.
- Кнопки примеров заполняют форму и выполняют настоящий запрос.
- Отличие отсутствующей категории от пустой выдачи и технической ошибки.
- Смену даты с сохранением остальных полей и объяснением исчезнувших карточек.
- «Оформление»: пресеты, собственный HEX, сохранение цвета и сброс.
- Адаптивный интерфейс на телефоне и широком экране.

До первого запроса карточек нет. Нажмите «Подобрать подрядчиков»: при исходных параметрах показываются Куррапика, Мицури Канроджи и Кики. Измените дату на 01.10.2026 и повторите запрос — Кики исключается из-за занятости, остаются две карточки. Календарное окно формы — 23.09.2026–31.12.2026. Бюджет задаётся в целых положительных тенге, длительность допускает дробные часы.

## Материалы и дальнейшая работа

- [Аудит исходных данных](docs/audit.md): 66 профилей, CSV как основной источник, сверка с HTML и DOCX.
- [Правила дизайна и замена логотипа](docs/design.md).
- [Этапы и разрешённый объём](docs/progress.md).
- `frontend/src/App.tsx` — форма, реальные результаты и состояния.
- `frontend/src/lib/use-matching.ts` — загрузка каталога, отмена запросов и защита от запоздалых ответов.
- `frontend/src/components/RecommendationCard.tsx` — сведения о подрядчике и раскрываемые основания.
- `frontend/src/styles/tokens.css` — оформление и цветовые переменные.
- `frontend/src/config/brand.ts` — логотип и временный favicon.

Используются React, TypeScript, Vite, Tailwind CSS, адаптированная кнопка shadcn/ui, React Hook Form, Zod и Playwright. Основа FastAPI/Pydantic и загрузка CSV/JSONL готовы. Фильтры и детерминированный порядок реализованы. Объяснения и публичный API подбора реализованы на этапе 5. Реальные карточки подключены к фронтенду на этапе 6. NVIDIA предусмотрен только как необязательное дальнейшее улучшение. В этапе 7 измерено время от отправки формы до готовой выдачи: в пяти локальных повторах среднее 49.84 мс, максимум 52.7 мс. Начальная загрузка страницы не включена; эти результаты не характеризуют внешний хостинг или нагрузку.

Оригинальные CSV, HTML, DOCX и JPEG сохранены без изменений. Документы и снимки этапа 2 относятся к интерфейсу; проверки алгоритма подбора описаны отдельно в материалах этапа 4.


## Сервер и каталог

Проверенная среда сервера: Linux, Python 3.13.5. Точные версии всех установленных зависимостей закреплены в `backend/requirements.txt`. Из корня репозитория:

```bash
python3 -m venv backend/.venv
backend/.venv/bin/python -m pip install -r backend/requirements.txt
backend/.venv/bin/python -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8000
```

Адреса:

- http://localhost:8000/health — готовность каталога, число записей и версия.
- http://localhost:8000/api/catalog/meta — справочники, окно календаря, статистика качества и происхождение.
- http://localhost:8000/docs — описание доступных технических API.

При ошибке загрузки оба рабочих endpoint возвращают HTTP 503; подробности с файлом, строкой, id и полем доступны в журнале сервера и CLI. Это не бизнес-исход пустого подбора. POST /api/recommendations возвращает до трёх карточек с проверяемыми объяснениями; CLI ниже сохраняет внутреннюю диагностику этапа 4. Браузер использует Vite proxy для `/api`; отдельная настройка CORS в локальном режиме не нужна.

Проверка каталога и тесты, из `backend/`:

```bash
.venv/bin/python -m app.cli
.venv/bin/python -m pytest -q
```

`app.cli` возвращает JSON-отчёт, при ошибке — exit code 1 и JSON-диагностику в stderr. Пример текущего отчёта: [catalog-report.json](docs/catalog-report.json). [Правила импорта и происхождения](docs/catalog.md).

## CSV, JSONL и отдельные фикстуры

По умолчанию сервер читает только `hackathon dataset anonymized .csv` из корня репозитория. Путь `CATALOG_PATH` задаётся относительно корня проекта или абсолютным путём. Дополнительные источники автоматически не объединяются.

Из `backend/` можно воспроизводимо создать новый производный файл:

```bash
.venv/bin/python -m app.cli --export data/derived/new-catalog.jsonl
.venv/bin/python -m app.cli data/derived/new-catalog.jsonl
```

Вместе с JSONL создаётся `.meta.json` с происхождением каждого профиля. Переносите оба файла вместе. Существующие файлы экспорт не перезаписывает. В репозитории уже сохранён проверенный экспорт `data/derived/catalog.jsonl`; он является другой формой исходного CSV, а не дополнительным набором.

Запуск сервера с JSONL из корня:

```bash
CATALOG_PATH=data/derived/catalog.jsonl backend/.venv/bin/python -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8000
```

Изолированная проверка демофикстур, из `backend/`:

```bash
.venv/bin/python -m app.cli backend/tests/fixtures/catalog_valid.jsonl --origin demo
```

Отдельный сервер с тестовым каталогом, из корня:

```bash
CATALOG_PATH=backend/tests/fixtures/catalog_valid.jsonl CATALOG_ORIGIN=demo backend/.venv/bin/python -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8001
```

Такой каталог содержит два вымышленных профиля и не смешивается с оригиналом. Это проверка загрузки данных, не полноценное демо рекомендаций. Для просмотра этого режима запустите интерфейс с `API_PROXY_TARGET=http://127.0.0.1:8001 npm run dev`: он получит справочники выбранного каталога и покажет постоянный баннер «Демонстрационные синтетические данные». Для отдельно добавленных командой записей есть явное происхождение `team`; оно требует `synthetic: true`. Флаг `synthetic` сам по себе не определяет происхождение.

Сведения о стадиях, фактических проверках и ограничениях: [progress.md](docs/progress.md).


## Подбор: этап 4

Из `backend/`:

```bash
.venv/bin/python -m app.match_cli --query docs/queries/corporate-host.json
```

Для Алматы, ведущего, корпоратива 30.09.2026 с бюджетом 1 000 000 ₸ проходят шесть профилей; первые три — HK-88430, HK-44923, HK-35215. Файл `corporate-host-next-date.json` меняет дату на 01.10.2026: остаются HK-88430 и HK-44923, Кики занят.

[Правила и все семь сценариев](docs/matching.md), [вычисленный отчёт](docs/matching-report.json).

Условия обязательны: город и категория, доступность на дату, формат, бюджет, язык и длительность при их указании. Затем сортировка по начальной цене и каноническому id; вывод до трёх профилей. Счётчики основных причин не учитывают одного исключённого кандидата дважды. Результаты MATCHED, CATEGORY_UNAVAILABLE, NO_MATCHES отделены от ошибок валидации и загрузки.

Подбор не использует AI и не оценивает качество подрядчика по цене. CLI этапа 4 показывает диагностику; публичный API этапа 5 дополнительно формирует объяснения. Сайт использует публичный API. Примеры CLI — диагностические результаты, а не готовый контракт браузерного API.


## Объяснения и API: этап 5

При запущенном сервере, из корня проекта:

```bash
curl -X POST http://localhost:8000/api/recommendations \
  -H 'Content-Type: application/json' \
  --data-binary @docs/queries/corporate-host.json
```

[Контракт API, сериализация и правила объяснений](docs/api.md). Реальные сохранённые ответы: [основной запрос](docs/api-examples/corporate-host.json), [пустая выдача](docs/api-examples/no-matches.json), [отсутствующая категория](docs/api-examples/no-category.json), [минимальная длительность фотобудки](docs/api-examples/minimum-duration.json).

Объяснение объединяет подтверждённые условия запроса с точной атрибутированной цитатой. Для исходного набора вручную размечен 61 содержательный фрагмент; ещё 5 описаний отмечены как недостаточные. Хэш описания и контекст полей защищают от использования устаревшей разметки. При неизвестном/изменённом профиле остаются структурированные факты и понятное ограничение. Внешняя модель не вызывается.

Валидные бизнес-исходы возвращают 200, ошибки запроса — 422, незагруженный каталог — 503, внутренний сбой — 500. Тексты ошибок не содержат исходный запрос или трассировку.

Из `backend/` можно обновить схему, типы TypeScript и примеры:

```bash
.venv/bin/python -m app.export_contract
.venv/bin/python -m app.export_examples
```

Фронтенд получает типы из OpenAPI и вызывает fetch-клиент с AbortSignal. При новом подборе старые карточки сразу убираются; предыдущий запрос отменяется, а его поздний ответ игнорируется. Таймаут 12 секунд показывает ошибку с возможностью повтора. Смена цвета не вызывает API и сохраняет форму, выдачу и раскрытые основания.

При редактировании параметров до повторного запроса прежняя выдача подписана как результат старых условий. Сравнение дат использует `eligible_ids` и `exclusions` сервера и выполняется только при неизменных остальных параметрах, версии каталога и порядке подбора. Клиент не пересчитывает фильтры или сортировку.

Проверки этапа 6: сборка TypeScript/Vite прошла; 9 браузерных сценариев прошли за 18.7 с. После локальной правки карточки на 320 px повторены только сборка и один сценарий адаптивности, успешно. Подробности и ограничения — в [progress.md](docs/progress.md).


## Проверка собранного сайта и замеры: этап 7

```bash
cd frontend
npm run test:stage7
```

Команда собирает сайт и проверяет production preview на 4174 с отдельным сервером на 8002. Перед запуском нужны установленные Python/npm-зависимости и Chromium. Снимки предыдущих этапов сохраняются; новые имеют префикс `stage7-`. Семь исходных запросов, пять повторов и измерения в браузере записываются в `docs/stage7-browser-report.json`. Методика, сценарии и границы проверки: [отчёт этапа 7](docs/verification-stage7.md).

Итог этапа 7: 247 серверных и 15 браузерных проверок прошли, все 7 демонстрационных сценариев подтверждены. Отправка формы → готовая выдача: среднее 49.84 мс в пяти локальных повторах; условия и исходные замеры в отчёте. Финальная подготовка документации и повтор запуска по README относятся к этапу 8 и ещё не выполнены.
