# Повторная проверка по ТЗ — 23.09.2026

Основание: исходное задание DOCX, код и новый запуск на Linux, Python 3.13.5, Node 22.22.0, Chromium 153.0.8010.12.

## Результат

- `pytest -q`: 247 passed, 1 предупреждение об устаревании httpx в Starlette TestClient.
- `npm run build`: успешно.
- `PLAYWRIGHT_PRODUCTION=1 npm run test:e2e`: 15 passed (41.1 s).
- API готов, загружены 66 профилей.
- Через работающий dev-сайт проверены 7 запросов: плотная категория, другая дата, редкая категория, отсутствие подходящих, отсутствие категории, свободная и занятая площадка. Все результаты совпали с ожидаемыми; локальные HTTP-запросы заняли 4.8–12.1 мс, без учёта отрисовки.
- Браузерные тесты подтвердили повторяемость, объяснение смены даты, пустые состояния, обработку ошибок, адаптивность и настройки оформления.

Новую машинную проверку и снимки сохранили рядом; исторические отчёты этапа 7 не изменены. Исходный код не менялся.

## Соответствие и ограничения

Все шесть функциональных требований исходного ТЗ реализованы и покрыты выполненными проверками. Для демонстрационных сценариев Definition of Done подтверждён. Нельзя гарантировать различимость объяснений для любой комбинации: у 5 профилей исходные описания недостаточно содержательны; для новых профилей нет автоматического выделения особенностей. Для таких случаев предусмотрены предупреждения и структурированные сведения.

Внешнего LLM нет; объяснения собираются из проверенных фактов. Порядок — начальная цена и id, а не смысловая релевантность. Это допустимое базовое решение, но ограничивает AI-составляющую и потенциал рекомендаций. Docker-файлов пока нет. Календарь фиксирован на 23.09–31.12.2026.

## Запущено для просмотра

Интерфейс: http://localhost:5173. API: http://localhost:8000/docs.

В этой среде отсутствовали Node/npm и ensurepip. Node установлен временно в `/tmp/node-v22.22.0-linux-x64`, зависимости Python — в `backend/.venv`. Для повторного ручного запуска фронтенда здесь: `PATH=/tmp/node-v22.22.0-linux-x64/bin:$PATH npm run dev -- --host 127.0.0.1 --port 5173 --strictPort` из frontend. `/tmp` не является постоянной установкой Node.

## Предлагаемый следующий этап

1. Docker Compose: backend FastAPI и frontend со сборкой и Nginx, маршрут `/api`, healthcheck, каталог только для чтения, инструкция запуска одной командой. Для текущих 66 записей база не нужна.
2. Повысить индивидуальность объяснений; при подключении LLM извлекать только проверяемые факты, сохранять локальный fallback и детерминированный порядок.
3. При пустой выдаче вычислять конкретные альтернативы: ближайшая свободная дата или минимальный достаточный бюджет, с повторной проверкой фильтров.
4. Добавить сравнение трёх вариантов, сохранение параметров и ссылку на подбор.

Дизайн: развить текущий светлый интерфейс — молочный фон, графитовый текст, синий акцент логотипа, компактная закреплённая форма слева, карточки справа. Выделить одну отличительную причину выбора крупнее, общие совпадения показать короткими метками, основания раскрывать. На мобильном экране свернуть заполненные параметры над выдачей. Уменьшить высоту вводного блока, чтобы результаты раньше попадали на экран.
